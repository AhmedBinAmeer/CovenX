import type { ReactNode } from 'react';
import { Bell, BriefcaseBusiness, ChevronRight, ClipboardCheck, FileCheck2, FileText, Gauge, LogOut, Settings as SettingsIcon, Sparkles } from 'lucide-react';
import { useAuth } from '../main';
const links = [{ path: '/', label: 'Overview', icon: Gauge }, { path: '/contracts', label: 'Contracts', icon: FileText }, { path: '/approvals', label: 'Approvals', icon: ClipboardCheck }, { path: '/obligations', label: 'Obligations', icon: BriefcaseBusiness }, { path: '/documents', label: 'Documents', icon: FileCheck2 }, { path: '/notifications', label: 'Notifications', icon: Bell }];
export function Layout({ path, setPath, onLogout, children }: { path: string; setPath: (p: string) => void; onLogout: () => void; children: ReactNode }) { const { user } = useAuth(); const display = user?.profile?.displayName ?? user?.email ?? 'Workspace user'; const initials = display.split(' ').map((x: string) => x[0]).join('').slice(0, 2).toUpperCase(); const go = (p: string) => { window.history.pushState({}, '', p); setPath(p); }; return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">C</div><div className="brand-name">CovenX</div></div><div className="nav-label">Workspace</div><nav className="nav-group">{links.map(({ path: itemPath, label, icon: Icon }) => <button key={itemPath} className={`nav-item ${path === itemPath ? 'active' : ''}`} onClick={() => go(itemPath)}><Icon size={17} /><span>{label}</span></button>)}</nav><div className="nav-label">Administration</div><button className={`nav-item ${path === '/settings' ? 'active' : ''}`} onClick={() => go('/settings')}><SettingsIcon size={17} /><span>Settings</span></button><div className="sidebar-footer"><div className="user-mini"><div className="avatar">{initials}</div><div style={{ minWidth: 0 }}><div style={{ color: '#fff', fontSize: 12, fontWeight: 700, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{display}</div><div style={{ color: '#7893a8', fontSize: 10, marginTop: 3 }}>Enterprise workspace</div></div><button aria-label="Sign out" className="icon-button" onClick={onLogout} style={{ marginLeft: 'auto', background: 'transparent', borderColor: 'transparent', color: '#92aabd' }}><LogOut size={15} /></button></div></div></aside><main className="main-panel"><header className="topbar"><div className="breadcrumb"><span>CovenX</span><ChevronRight size={14} style={{ verticalAlign: 'middle', margin: '0 6px' }} /><strong>{links.find((x) => x.path === path)?.label ?? 'Settings'}</strong></div><div style={{ display: 'flex', alignItems: 'center', gap: 10 }}><div className="badge badge-green"><Sparkles size={12} /> Live workspace</div><button className="icon-button" aria-label="Notifications" onClick={() => go('/notifications')}><Bell size={17} /></button></div></header><div className="content">{children}</div></main></div>; }
