import { useEffect, useState } from 'react';
import { Download, FileCheck2, Lock } from 'lucide-react';
import { endpoints } from '../services/api';
export function Documents() { const [items, setItems] = useState<any[]>([]); useEffect(() => { endpoints.documents('all').then((d: any) => setItems(Array.isArray(d) ? d : (d && d.data) || [])).catch(() => setItems([])); }, []); return <><div className="page-heading"><div><div className="eyebrow">Secure repository</div><h1>Documents</h1><p className="subtitle">Private, scanned contract files with controlled access.</p></div><div className="badge badge-blue"><Lock size={12} /> Private storage</div></div><div className="notice" style={{ marginBottom: 18 }}>Documents are served through short-lived authorized URLs. File contents and storage keys are never exposed in notifications or realtime events.</div><section className="card table-card"><div className="table-scroll">{items.length === 0 ? <div className="empty"><FileCheck2 size={25} style={{ display: 'block', margin: '0 auto 12px', color: '#1d9365' }} />No document records in this view.</div> : <table><thead><tr><th>File</th><th>Type</th><th>Scan</th><th>Uploaded</th><th /></tr></thead><tbody>{items.map((d: any) => <tr key={d._id}><td><strong>{d.fileName}</strong><div style={{ color: '#8293a0', fontSize: 11, marginTop: 4 }}>{d.classification}</div></td><td>{d.mimeType}</td><td><span className="badge badge-green">{d.scanStatus}</span></td><td>{d.createdAt ? new Date(d.createdAt).toLocaleDateString() : '—'}</td><td><button className="icon-button" aria-label="Download document" onClick={() => endpoints.documents(d._id)}><Download size={15} /></button></td></tr>)}</tbody></table>}</div></section></>; }
