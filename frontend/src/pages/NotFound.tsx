import { ArrowLeft, ShieldAlert } from 'lucide-react';
export function NotFound({ navigate, denied = false }: { navigate: (path: string) => void; denied?: boolean }) { return <section className="card panel" style={{ maxWidth: 680, margin: '40px auto', textAlign: 'center' }}><ShieldAlert size={34} color={denied ? '#be5a54' : '#e68a36'} style={{ marginBottom: 14 }} /><h1>{denied ? 'Access restricted' : 'Workspace page not found'}</h1><p className="subtitle">{denied ? 'Your current enterprise role does not have permission to view this resource.' : 'The requested CovenX workspace route does not exist.'}</p><button className="btn btn-primary" onClick={() => navigate('/')}><ArrowLeft size={15} /> Return to overview</button></section>; }
