import { useEffect, useState } from 'react';
import { CheckCircle2 } from 'lucide-react';
import { endpoints } from '../services/api';
export function Obligations() { const [items, setItems] = useState<any[]>([]); useEffect(() => { endpoints.obligations().then((d: any) => setItems(Array.isArray(d) ? d : (d && d.data) || [])).catch(() => setItems([])); }, []); return <><div className="page-heading"><div><div className="eyebrow">Compliance monitoring</div><h1>Obligations</h1><p className="subtitle">Keep recurring commitments visible, owned, and on time.</p></div><div className="badge badge-green"><CheckCircle2 size={12} /> Compliance workspace</div></div><section className="card table-card"><div className="table-scroll">{items.length === 0 ? <div className="empty">No obligations are currently assigned.</div> : <table><thead><tr><th>Obligation</th><th>Owner</th><th>Due date</th><th>Priority</th><th>Status</th></tr></thead><tbody>{items.map((o: any) => <tr key={o._id}><td><strong>{o.title}</strong><div style={{ color: '#8293a0', fontSize: 11, marginTop: 4 }}>{o.type}</div></td><td>{o.ownerId}</td><td>{o.dueDate ? new Date(o.dueDate).toLocaleDateString() : '—'}</td><td><span className={`badge badge-${o.priority === 'critical' || o.priority === 'high' ? 'red' : 'slate'}`}>{o.priority}</span></td><td><span className={`badge badge-${o.status === 'completed' ? 'green' : o.status === 'overdue' ? 'red' : 'orange'}`}>{o.status}</span></td></tr>)}</tbody></table>}</div></section></>; }
