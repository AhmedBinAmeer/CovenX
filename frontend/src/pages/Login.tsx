import { FormEvent, useState } from 'react';
import { ArrowRight, LockKeyhole, ShieldCheck } from 'lucide-react';
import { useAuth } from '../main';
export function Login() { const { login } = useAuth(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); const submit = async (event: FormEvent) => { event.preventDefault(); setBusy(true); setError(''); try { await login(email, password); } catch (e: any) { setError(e.message ?? 'Unable to sign in'); } finally { setBusy(false); } }; return <div className="login-layout"><section className="login-hero"><div><div className="brand login-brand"><img className="brand-logo brand-logo-login" src="/covenx-logo-transparent.png" alt="CovenX Enterprise Contract Lifecycle Management Platform" /></div><div className="login-hero-copy"><div className="eyebrow">Contract intelligence, secured</div><h1>Move every agreement forward with confidence.</h1><p className="login-hero-lede">A trusted enterprise workspace for creating, reviewing, approving, signing, monitoring, and renewing contracts.</p></div></div><div className="login-footer-meta"><span><ShieldCheck size={14} style={{ verticalAlign: 'middle', marginRight: 5 }} /> Secure by design</span><span>© CovenX Enterprise</span></div></section><section className="login-form-section"><form onSubmit={submit} className="card" style={{ padding: 'clamp(26px, 5vw, 48px)', width: 'min(430px, 100%)' }}><div className="eyebrow">Welcome back</div><h2 style={{ fontSize: 28, marginTop: 8 }}>Sign in to CovenX</h2><p className="subtitle" style={{ lineHeight: 1.6 }}>Access your enterprise contract workspace.</p><div style={{ display: 'grid', gap: 16, marginTop: 28 }}><div className="field"><label htmlFor="email">Work email</label><input id="email" type="email" autoComplete="email" required value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@company.com" /></div><div className="field"><label htmlFor="password">Password</label><div className="search" style={{ width: '100%' }}><LockKeyhole size={15} /><input id="password" type="password" autoComplete="current-password" required value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Enter your password" /></div></div>{error && <div className="notice">{error}</div>}<button className="btn btn-primary" disabled={busy}>{busy ? 'Signing in…' : <>Continue securely <ArrowRight size={16} /></>}</button></div><div style={{ marginTop: 24, color: '#91a0aa', fontSize: 11, lineHeight: 1.6 }}>Your session is protected with short-lived access tokens and rotating refresh credentials.</div></form></section></div>; }
