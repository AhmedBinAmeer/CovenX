import { Contract, ContractVersion, CollaborationComment, DocumentChunk, User, Document, Notification, DashboardProjection, IntakeRequest, IntakeQuestionnaire, Integration, WebhookEvent } from '../models/index.js';
export async function verifyIndexes() { await Promise.all([User.createIndexes(), Contract.createIndexes(), ContractVersion.createIndexes(), CollaborationComment.createIndexes(), DocumentChunk.createIndexes(), Document.createIndexes(), Notification.createIndexes(), DashboardProjection.createIndexes(), IntakeRequest.createIndexes(), IntakeQuestionnaire.createIndexes(), Integration.createIndexes(), WebhookEvent.createIndexes()]); return true; }
export async function migrationStatus() { return { version: 1, indexesVerified: true, destructiveMigrations: false }; }
